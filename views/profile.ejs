<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Harshit</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-end w-full">
        <a href="/logout" class="px-3 py-2 bg-red-500 border-red-800 rounded-lg hover:bg-red-700"
          >Logout</a
        >
      </div>
      <div class="flex items-start gap-5">
        <div class=" w-20 h-20 bg-white rounded-[52%] overflow-hidden">
          <img class="w-full h-full object-cover " src="/images/<%= user.profilepic %>" alt=""> 
        </div>
      <h3 class="text-3xl mb-5 mt-5 ">
        Hello, <span class="text-purple-400"> <%= user.name %> </span> 🤴🏽
      </h3>
    </div>
    <h5 class="m-5">You Can Create a post.</h5>
      <form action="/post" method="post">
        <textarea
          class="resize-none bg-transparent border-2 border-zinc-800 outline-none rounded-md p-3 w-1/3"
          name="content"
          placeholder="What's in your mind..."
        ></textarea>
        <input
          type="submit"
          value="Create New Post"
          class="bg-blue-500 px-3 py-2 block rounded-lg mt-2 w-1/3"
        />
      </form>

      <div class="posts mt-20">
        <h3 class="text-zinc-400 mb-1">Your Posts</h3>
        <div class="postcontainer">
            <% user.posts.reverse().forEach(function(post){ %> 
                <div class="post w-1/2 bg-transparent p-5 border-[1px] rounded-lg border-zinc-800 mb-2">
                    <h3 class="text-blue-500">@<%= user.username %></h3>
                    <p class="text-grey-300 text-sm"><%= post.content %></p>
                    <small class="block text-red-300 m-1">likes <%= post.likes.length %></small>
                    <div class="btns flex justify-start gap-5">
                        <a href="/like/<%= post._id %>" class="mt-2 text-blue-500">
                          <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                        </a>
                        <a href="/edit/<%= post._id %>" class="mt-2 text-zinc-500">Edit</a>
                        <a href="/delete/<%= post._id %>" class="mt-2 text-red-500">Delete</a>
                    </div>
                </div>
            <% }) %>            
        </div>
      </div>
    </div>
  </body>
</html>
