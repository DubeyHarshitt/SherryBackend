<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Harshit</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-end w-full">
        <a href="/logout" class="px-3 py-2 bg-red-500 border-red-800 rounded-lg hover:bg-red-700"
          >Logout</a
        >
      </div>

        <h3 class="text-3xl mb-5 mt-10">
            Hello, <span class="text-purple-400"> <%= post.user.name %> </span> 🤴🏽
        </h3>
        <span class="text-yellow-400"> <%= post.user.username %> </span>

      <h5 class="mb-5">You Can Now Edit your post.</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          class="resize-none bg-transparent border-2 border-zinc-800 outline-none rounded-md p-3 w-1/3"
          name="content"
          placeholder="What's in your mind..."
        ><%= post.content %></textarea>
        <input
          type="submit"
          value=" Update Post"
          class="bg-yellow-500 text-black hover:bg-yellow-600 px-3 py-2 block rounded-lg mt-2 w-1/3"
        />
      </form>

    </div>
  </body>
</html>
